import { Action } from "../Action";
import { Item } from "../Item";

export class Actor {
    name: string = "test_actor";
    level: number = 0;
    loot: Item[] = [];
    dialogue: string = '';

    constructor() {
        } 
}