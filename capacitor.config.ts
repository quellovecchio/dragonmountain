import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.qqvvkk.dragonmountain',
  appName: 'DragonMountain',
  webDir: 'dist/dragon-mountain',
  server: {
    androidScheme: 'https'
  }
};

export default config;
